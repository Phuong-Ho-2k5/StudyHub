spring:
  config:
    import: optional:file:../.env[.properties]
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/studyhub}
    username: ${POSTGRES_USER:studyhub}
    password: ${POSTGRES_PASSWORD:}
